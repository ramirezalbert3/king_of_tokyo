import constants.py
import dice.py

class Player:
    nPlayers = 0
    def __init__():
        nPlayers = nPlayers + 1
        self.ID = nPlayers
        self.lives = constants.MAX_LIVES
        self.points = 0
        self.dice.Dice
        self.remainingRolls = ROLLS_PER_TURN

    def takeDamage(damagePoints):
        self.lives = self.lives - damagePoints

    def heal(healAmmount):
        self.lives = self.lives + healAmmount

    def processDice():
        

    def countPoints():